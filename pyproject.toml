[project]
name = "clima"
version = "0.1.0"
description = "Morning weather notification service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "pydantic-settings>=2.0.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
morning-weather = "main:main"
